<table id='shopping-cart'>
  <th>Item Name</th>
  <th>Number Ordered</th>
  <th>Price</th>
  <th>Remove</th>
  <% current_user.cart_items.each do |cart_item| %>
    <% item = cart_item.item %>
    <%= render partial: "cart_item", locals: {cart_item: cart_item, item: item} %>
  <% end %>
</table>
